//! dcs-ui — egui binary.
//!
//! View modes (grid, gallery; board later) + ephemeral UI state, over the
//! single command registry in dcs-app. Top of the dependency tree. (§9)

fn main() {
    println!("dcs — digital contact sheet (scaffold)");
}
